name: failure_demo_gpu
dataset: synthetic
gpu_type: T4
use_gpu: true
max_steps: 1000
max_epochs: 100
batch_size: 64
learning_rate: 0.001
checkpoint_every_steps: 100
resume: true
simulate_failure_at_step: 350
